# Ignore all raw and processed data files
data/raw/*
data/processed/*

# Except keep the folder structure alive
!data/raw/.gitkeep
!data/processed/.gitkeep

# Ignore model checkpoints because they can get large
models/*.pkl